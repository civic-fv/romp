#pragma once

#include <cstddef>
#include <string>
// #include <filesystem>

// what C++ type should we use for range values?
extern std::string value_type;

// what C++ type should we use for scalar values?
extern std::string scalar_type;

// the filepath of the murphi model 
// extern std::filesystem::path file_path;
