from lit.formats import ShTest
from os import path


config.name = "romp"

config.test_format = ShTest()
config.suffixes = [".m"]

config.substitutions = [
    (r"romp", f"\"{path.join(config.test_exec_root or path.join(config.test_source_root or path.join(path.dirname(__file__), '..'), 'build'), 'romp/romp')}\""),
    (r"c\+\+", "g++ -pipe -x c++ -std=gnu++17"),
  ]
